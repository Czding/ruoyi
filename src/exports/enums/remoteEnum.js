

/**
 * 接口导出key备注映射见文件同级README.md
*/

const getRemoteEnum = async () => {
  // const { data } = await sysDictTreeList()
  // const remoteEnum = {}
  // data.forEach(item => {
  //   const options = (item.children || []).map(o => ({ label: o.name, value: o.dataValue }))
  //   remoteEnum[item.dictCode] = options
  // })
  // return remoteEnum
  return {}
}

export default getRemoteEnum
